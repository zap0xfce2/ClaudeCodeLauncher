[project]
name = "claude-code-launcher"
version = "0.1.0"
description = "Curses-basierter Terminal-Launcher für Claude Code Sessions"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = ["nuitka>=4.1"]

[tool.uv]
package = false
